<ol class="breadcrumb">
    <li><a href="/">Home</a></li>
    <li class="active">Delete Recipe</a></li>
</ol>

<br/>

<%- partial ('partials/statusmessage.ejs') %>

<h2>Delete Recipe</h2>


<div style="text-align: center; padding-top:50px">
  <form action="/delete" method="POST">
    <div class="form-group">
      <label for="id">SELECT A RECIPE TO DELETE</label><br/>
      <select id="id" name="id" class="selectpicker" data-live-search="true" data-header="Select a recipe">
      <option value="">Choose a Recipe</option>
      <% if(typeof recipes != "undefined" && recipes.length > 0){ %>
        <% recipes.forEach(function(recipe){ %>
          <option value="<%= recipe.id %>">
            <%= recipe.title%>
          </option>
        <% }) %>
      <% } %>
      </select>
    </div>

    <div class="form-group">
      <button type="submit" class="btn btn-danger">
        DELETE RECIPE
        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
      </button>
    </div>
  </form>
</div>

<h2>Ingredients</h2>
<table class="table table-striped table-bordered" id="ingredientsTable" style="width:100%">
  <thead>
    <tr>
      <th></th>
      <th>I Name</th>
      <th>Units</th>
      <th>Q</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>

<h2>Instructions</h2>
<table class="table table-striped table-bordered" id="instructionsTable" style="width:100%">
  <thead>
    <tr>
      <th></th>
      <th>Step</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>
