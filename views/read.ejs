<ol class="breadcrumb">
    <li class="active">Home</a></li>
</ol>

<br/>

<%- partial ('partials/statusmessage.ejs') %>

<select class="selectpicker" name="recipe_id" data-live-search="true" data-header="Select a recipe" multiple data-actions-box="true" multiple title="Search">
<% if(typeof recipes != "undefined" && recipes.length > 0){ %>
  <% recipes.forEach(function(student){ %>
    <option value="<%= student.student_id %>" data-icon="glyphicon glyphicon-user">
      <%= student.last_name + ", " +  student.first_name %>
    </option>
  <% }) %>
<% } %>
</select>

  <!-- <div id="accordion" role="tablist" aria-multiselectable="true">
    <div class="card">
      <div class="card-header" role="tab" id="headingOne">
        <h5 class="mb-0">
          <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
            Collapsible Group Item #1
          </a>
        </h5>
      </div>
      <div id="collapseOne" class="collapse show" role="tabpanel" aria-labelledby="headingOne">
        <div class="card-block">
          This should collapse and go away.
        </div>
      </div>
    </div>
  </div>

  <div id="thisIsAnID">
      <a id="card">This is the card</a>
      <a id="text">This should collapse, also</a>
  </div> -->

      <div class="row">
       <div class="span4 collapse-group">
         <h5>Recipe</h5>
          <p class="collapse" id="viewdetails">This is a recipe!</p>
          <p><a class="btn" data-toggle="collapse" data-target="#viewdetails">Details &raquo;</a></p>
       </div>
     </div>



<h2>Recipes</h2>
<table class="table table-striped table-bordered" id="recipeTable" style="width:100%">
  <thead>
    <tr>
      <th>Recipe Name</th>
      <th>Description</th>
      <th>Preparation Time</th>
      <th>Ingredient List</th>
      <th>Instructions</th>
    </tr>
  </thead>
  <tbody>
  <% if(typeof recipes != "undefined" && recipes.length > 0){ %>
    <% recipes.forEach(function(recipe){ %>
      <tr>
        <td><%= student.student_id %>


        </td>
        <td><%= student.first_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.start_date %></td>
        <td><%= student.gpa %></td>
      </tr>
    <% }) %>
  <% } %>
  </tbody>
</table>
