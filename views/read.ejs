<ol class="breadcrumb">
    <li class="active">Home</a></li>
</ol>

<br/>

<%- partial ('partials/statusmessage.ejs') %>

<<<<<<< HEAD
=======
<select class="selectpicker" name="student_id" data-live-search="true" data-header="Select a student" multiple data-actions-box="true" multiple title="Search">
<% if(typeof students != "undefined" && students.length > 0){ %>
  <% students.forEach(function(student){ %>
    <option value="<%= student.student_id %>" data-icon="glyphicon glyphicon-user">
      <%= student.last_name + ", " +  student.first_name %>
    </option>
  <% }) %>
<% } %>
</select>

>>>>>>> master
<h2>Recipes</h2>
<table class="table table-striped table-bordered" id="studentTable" style="width:100%">
  <thead>
    <tr>
      <th>Recipe Name</th>
      <th>Description</th>
      <th>Preparation Time</th>
      <th>Ingredient List</th>
      <th>Instructions</th>
    </tr>
  </thead>
  <tbody>
  <% if(typeof students != "undefined" && students.length > 0){ %>
    <% students.forEach(function(student){ %>
      <tr>
        <td><%= student.student_id %></td>
        <td><%= student.first_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.start_date %></td>
        <td><%= student.gpa %></td>
      </tr>
    <% }) %>
  <% } %>
  </tbody>
</table>
